[workspace]
resolver = "2"
members = [
    "ncf-core",
    "ncf-io",
    "ncf-cli",
    "ncf-convert",
    "ncf-py",
    "fuzz",
]

[workspace.dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
